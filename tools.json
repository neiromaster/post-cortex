[
  {
    "name": "create_session",
    "description": "Create a new conversation session",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "load_session",
    "description": "Load session into active memory",
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": { "type": "string", "description": "Session ID to load" }
      },
      "required": ["session_id"]
    }
  },
  {
    "name": "list_sessions",
    "description": "List all sessions",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "search_sessions",
    "description": "Search sessions by name or description",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Search query" }
      },
      "required": ["query"]
    }
  },
  {
    "name": "update_conversation_context",
    "description": "Add context update to conversation (qa, decision_made, problem_solved, code_change)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": { "type": "string" },
        "interaction_type": { "type": "string" },
        "content": { "type": "object" }
      },
      "required": ["session_id", "interaction_type", "content"]
    }
  },
  {
    "name": "semantic_search_session",
    "description": "Semantic search within a session (auto-vectorizes if needed)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": { "type": "string" },
        "query": { "type": "string" }
      },
      "required": ["session_id", "query"]
    }
  },
  {
    "name": "get_structured_summary",
    "description": "Get comprehensive session summary",
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": { "type": "string" }
      },
      "required": ["session_id"]
    }
  },
  {
    "name": "semantic_search",
    "description": "Universal semantic search with scope (session, workspace, or global)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": { "type": "string" },
        "scope_type": { "type": "string" },
        "scope_id": { "type": "string" }
      },
      "required": ["query"]
    }
  },
  {
    "name": "create_workspace",
    "description": "Create a workspace for organizing related sessions",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" }
      },
      "required": ["name", "description"]
    }
  },
  {
    "name": "get_workspace",
    "description": "Get details about a specific workspace",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workspace_id": { "type": "string" }
      },
      "required": ["workspace_id"]
    }
  },
  {
    "name": "list_workspaces",
    "description": "List all available workspaces",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "delete_workspace",
    "description": "Delete a workspace",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workspace_id": { "type": "string" }
      },
      "required": ["workspace_id"]
    }
  },
  {
    "name": "add_session_to_workspace",
    "description": "Add a session to a workspace",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workspace_id": { "type": "string" },
        "session_id": { "type": "string" },
        "role": { "type": "string" }
      },
      "required": ["workspace_id", "session_id", "role"]
    }
  },
  {
    "name": "remove_session_from_workspace",
    "description": "Remove a session from a workspace",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workspace_id": { "type": "string" },
        "session_id": { "type": "string" }
      },
      "required": ["workspace_id", "session_id"]
    }
  },
  {
    "name": "get_tool_catalog",
    "description": "Get comprehensive catalog of all available tools",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  }
]
